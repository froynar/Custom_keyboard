namespace Custom_keyboard.Localization;

/// <summary>Languages the UI can be displayed in. Persisted by name via <see cref="UserPreferencesStore"/>.</summary>
public enum AppLanguage
{
    Vietnamese,
    English
}
