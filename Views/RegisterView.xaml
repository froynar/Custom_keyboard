<UserControl x:Class="Custom_keyboard.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:Custom_keyboard.Behaviors">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#EEF2F6" Offset="0"/>
                <GradientStop Color="#F7F7F7" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border Width="460"
                Padding="30"
                Style="{StaticResource DashboardCard}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Border Width="40" Height="40" CornerRadius="8"
                            Background="{StaticResource AccentBrush}" Margin="0,0,12,0">
                        <TextBlock Text="&#xE765;" FontFamily="Segoe MDL2 Assets"
                                   Foreground="White" FontSize="20"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="CK Builder" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Custom Keyboard" FontSize="11"
                                   Foreground="{StaticResource TextMutedBrush}"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="Dang ky buyer"
                           FontSize="{StaticResource FontSizeHero}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Tao tai khoan de bat dau build keyboard"
                           Margin="0,4,0,22"
                           FontSize="14"
                           Foreground="{StaticResource TextMutedBrush}"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,6,0,12"/>

                <TextBlock Text="Email" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,6,0,12"/>

                <TextBlock Text="Phone" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,6,0,12"/>

                <TextBlock Text="Mat khau" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <PasswordBox behaviors:PasswordBoxBinding.Attach="True"
                             behaviors:PasswordBoxBinding.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,6,0,12"/>

                <TextBlock Text="Xac nhan mat khau" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <PasswordBox behaviors:PasswordBoxBinding.Attach="True"
                             behaviors:PasswordBoxBinding.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,6,0,12"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap"
                           MinHeight="22"/>

                <Button Content="Dang ky"
                        Command="{Binding RegisterCommand}"
                        Margin="0,8,0,10"
                        Style="{StaticResource PrimaryButton}"/>

                <Button Content="Quay lai dang nhap"
                        Command="{Binding ShowLoginCommand}"
                        Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
