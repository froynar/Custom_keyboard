<UserControl x:Class="Custom_keyboard.Views.SellerDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Custom_keyboard.Views"
             Loaded="OnLoaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,16">
            <Button DockPanel.Dock="Right" Content="Dang xuat"
                    Command="{Binding LogoutCommand}"
                    Padding="14,8" Background="#EEF2F6" BorderBrush="#DDE2E8"/>
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Foreground="#20242B"/>
                <TextBlock Text="{Binding Subtitle}" Margin="0,4,0,0" Foreground="#687381"/>
            </StackPanel>
        </DockPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Request">
                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="380"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Background="White" BorderBrush="#DDE2E8" BorderThickness="1" Padding="12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <DockPanel Margin="0,0,0,8">
                                <Button DockPanel.Dock="Right" Content="Refresh"
                                        Command="{Binding RefreshCommand}"
                                        Padding="10,4" Background="#EEF2F6" BorderBrush="#DDE2E8"/>
                                <TextBlock Text="Request duoc gan" FontWeight="SemiBold" FontSize="15" VerticalAlignment="Center"/>
                            </DockPanel>

                            <ListBox Grid.Row="1"
                                     ItemsSource="{Binding Requests}"
                                     SelectedItem="{Binding SelectedRequest}"
                                     BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,4">
                                            <TextBlock Text="{Binding RequestId}" FontWeight="SemiBold"/>
                                            <TextBlock FontSize="12" Foreground="#687381">
                                                <Run Text="Build:"/>
                                                <Run Text="{Binding BuildId}"/>
                                                <Run Text=" | "/>
                                                <Run Text="{Binding Status}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <WrapPanel Grid.Row="2" Margin="0,10,0,0">
                                <Button Content="Accept" Command="{Binding AcceptCommand}" Margin="0,0,8,8" Padding="12,6"
                                        Background="#2F6FED" Foreground="White" BorderThickness="0"/>
                                <Button Content="In progress" Command="{Binding StartProgressCommand}" Margin="0,0,8,8" Padding="12,6"
                                        Background="#EEF2F6" BorderBrush="#DDE2E8"/>
                                <Button Content="Complete" Command="{Binding CompleteCommand}" Margin="0,0,8,8" Padding="12,6"
                                        Background="#1F9D5B" Foreground="White" BorderThickness="0"/>
                                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,8,8" Padding="12,6"
                                        Background="#E5484D" Foreground="White" BorderThickness="0"/>
                            </WrapPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="White" BorderBrush="#DDE2E8" BorderThickness="1" Padding="12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="Snapshot build" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,8"/>
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <TextBox Text="{Binding SelectedRequestPayload, Mode=OneWay}"
                                         IsReadOnly="True" TextWrapping="Wrap"
                                         BorderThickness="0" FontFamily="Consolas" Background="#F8FAFC"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Chat">
                <Grid Margin="0,12,0,0">
                    <views:ChatView DataContext="{Binding Chat}"/>
                </Grid>
            </TabItem>
        </TabControl>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,12,0,0" Foreground="#687381"/>
    </Grid>
</UserControl>
